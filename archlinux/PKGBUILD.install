# shellcheck shell=bash

post_install() {
    systemctl --global --no-reload preset qubes-notification-agent.service || :
}

post_upgrade() {
    post_install
}

pre_remove() {
    systemctl --global --no-reload disable qubes-notification-agent.service || :
}
